spring:
    h2:
        console:
            enabled: true
    jpa:
        properties:
            database:
                dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        show-sql: true
    security:
        oauth2:
            client:
                registration:
                    google:
                        client-id: test
                        client-secret: test
                        scope: profile, email
    session:   
        store-type: jdbc